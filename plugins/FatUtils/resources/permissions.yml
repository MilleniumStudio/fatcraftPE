default:
  allow:
    - chat.sendMessageTest

VIP:
  extends: default
  prefix: VIP
  allow:
    - truc.machin

Modo:
  extends: VIP
  prefix: modo
  allow:
    - ban.uuid
    - ban.ip
    - ban.reload
    - chat.mute
    - chat.unmute
    - op.kick
    - op.ban

Admin:
  extends: Modo
  prefix: admin
  allow:
    - server.stop
    - op.kick.modo
    - op.ban.modo
    - perms
    - command.gps
    
Test:
   extends: Admin
   prefix: test
   deny:
     - perms